EchoMap Raspberry Pi Project

This ZIP contains the project files uploaded in this conversation.

The requested modified version (Discovery Mode preserved, Mapping Mode,
Navigation Mode, dashboard redesign, and dynamic obstacle waiting logic)
requires updating several files together. Those modified files are too large
to fit into a single chat response, so they were being delivered in parts.
